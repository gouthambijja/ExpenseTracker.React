import React from "react";

const Error = ({
  title = "Error!",
  message = "Something went wrong please reload the page or go to the previous page!",
}) => {
  return <div>Error</div>;
};

export default Error;
